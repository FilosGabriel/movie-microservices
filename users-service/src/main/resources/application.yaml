mongock:
  change-logs-scan-package:
    - com.filos.users.repository.migrations

spring:
  data:
    mongodb:
      database: users
      host: localhost
      port: 27017
      auto-index-creation: true

